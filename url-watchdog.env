# Este fichero se mantiene únicamente para compatibilidad con install.sh (instalación nativa).
# Para despliegue con Docker, usa .env.example como plantilla:
#
#   cp .env.example .env
#   nano .env
#   docker compose up -d
#
# Las variables del watchdog están documentadas en .env.example.
